# ============================================
# Node.js Backend Environment Variables
# ============================================

# Server Configuration
NODE_ENV=production
PORT=3000

# API Security
# IMPORTANT: Change this to a strong, random API key in production!
# Generate with: openssl rand -hex 32
API_KEY=change-me-in-production

# Python Detection Service Configuration
PYTHON_EXECUTABLE=/app/python/venv/bin/python
YOLO_MODEL_PATH=/app/models/my_model/train/weights/best.pt
MIN_CONFIDENCE=0.5

# Python Cache/Home Directories (for Docker/containerized deployments)
# These are set automatically in Dockerfile, but can be overridden
HOME=/app
APP_DIR=/app
RUNTIME_DIR=/app
MPLCONFIGDIR=/app/.config/matplotlib
XDG_CACHE_HOME=/app/.cache
XDG_CONFIG_HOME=/app/.config
EASYOCR_CACHE_DIR=/app/.EasyOCR
